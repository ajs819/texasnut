{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper">
		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
			<h1>{{ post.title }}</h1>
			{{ post.content }}
		</article>
	</div><!-- /content-wrapper -->
{% endblock %}